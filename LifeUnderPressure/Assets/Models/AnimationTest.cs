using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AnimationTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //GetComponent<Animator>().SetFloat("FrontBack", 1); // makes controller go forward
        //GetComponent<Animator>().SetFloat("FrontBack", -1); // makes controller go backwards
        //GetComponent<Animator>().SetFloat("LeftRight", 1); // makes controller go right
       // GetComponent<Animator>().SetFloat("LeftRight", -1); // makes controller go Left
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
